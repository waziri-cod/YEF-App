# Ignore generated UI components with lint rules that conflict with project settings
src/components/ui/**

# Ignore backend code (separate linting is handled in backend project)
backend/**

# Ignore configuration files that trigger lint errors
tailwind.config.ts

# Ignore specific services that use any for now
src/services/aiService.ts
